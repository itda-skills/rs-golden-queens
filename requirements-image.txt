# 이미지 발송 모드(MARKET_FLOW_RENDER=image)용 추가 의존성.
# 기본 텍스트 모드만 사용하는 환경(GitHub Actions 등)은 설치 불필요.
#
# 사용법:
#   pip install -r requirements.txt -r requirements-image.txt
#
# 추가로 시스템 Chrome (또는 Chromium) 이 필요하다.
-r requirements.txt
Jinja2>=3.1
html2image>=2.0
Pillow>=10.0
